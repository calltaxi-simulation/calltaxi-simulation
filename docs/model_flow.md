# 모델 흐름도

**대응 STRESS 항목: 2.1 기본 모델 개요 다이어그램**
(프로세스 흐름도·활동주기도 또는 그에 상응하는 다이어그램을 하나 이상 포함하라.
본문에는 복잡한 다이어그램을 피하라. 목표는 연구 대상 시스템에 대해 모델의 **폭과
깊이**를 전달하는 것이다)

서울 장애인콜택시 배차·운행 하루를 재생하는 이산사건 모델의 프로세스 흐름이다.
개체는 **콜**과 **차량** 둘이고, 아래 흐름은 두 개체가 만나는 지점(배차)을 축으로 그렸다.

---

## 프로세스 흐름

```mermaid
flowchart TD
    S(["조별 차고지 출차"]) --> IDLE

    C(["콜 접수"]) --> Q["점수제 대기열"]
    Q -->|"인내심 초과"| X(["취소 · 이탈"])
    Q --> D{{"배차"}}
    IDLE["하차지 인근 제자리 대기"] --> D

    D --> M["공차 이동"]
    M --> B["승차"]
    B --> R["운행"]
    R --> O["하차"]
    O --> IDLE

    IDLE -->|"근무 종료"| E(["차고지 복귀"])

    style X fill:#f8d7da,stroke:#a94442
    style E fill:#e2e3e5,stroke:#6c757d
    style S fill:#d4edda,stroke:#3c763d
    style C fill:#d4edda,stroke:#3c763d
```

**읽는 법**

- 왼쪽 위 `조별 차고지 출차` 는 **차량**의 진입점, `콜 접수` 는 **콜**의 진입점이다.
- 두 개체는 `배차` 에서 만난다. 차량은 `하차지 인근 제자리 대기` 에서, 콜은
  `점수제 대기열` 에서 배차를 기다린다.
- `하차 → 제자리 대기 → 배차` 가 **순환 고리**다. 차량은 근무 시간 동안 이 고리를 돈다.
  **차고지로 돌아가지 않는다** — 하차한 자리 근처에서 다음 콜을 기다린다(가정 A-01).
- **이탈 경로는 하나다.** 대기열에서 인내심을 초과한 콜이 `취소·이탈` 로 빠진다.
  차량 쪽에는 이탈이 없다(고장·결행을 모델링하지 않는다).
- 차량은 근무 종료 시각에만 `차고지 복귀` 로 모델을 벗어난다.

---

## 조 편성 (실측 재구성, 차량-일 20.3만 건)

차량의 진입 시각과 근무 길이는 가정이 아니라 실측에서 역추정한 값이다.
`calltaxi_2025_merged.csv` → `load.load_vehicle_trips()` → `metrics.vehicle_day_table()`
로 만든 **차량-일 202,880건**의 첫 승차·마지막 하차 분포다.

| 조 | 출차 시각 | 차량-일 비중 | 조별 종료 중앙 |
|---|---|---|---|
| 1조 | 07시 | **25.7%** | 14시 |
| 2조 | 08시 | **20.8%** | 15시 |
| 3조 | 10시 | **11.8%** | 18시 |
| 4조 | 12시 | **19.9%** | 18시 |
| 5조 | 13시 | **5.0%** | 19시 |

근무 길이 **6~8시간**. 차량-일 가동시간(`span_h`) 중앙 6.95시간, 사분위 5.66~7.91시간
으로 정합적이다.

위 5개 시각이 차량-일의 **83.2%** 를 덮는다. 나머지는 06시 2.8%, 09시 2.7%, 15시 2.7%,
19시 1.6% 등으로 흩어져 있다.

**한계** — 가동시간을 첫 승차~마지막 하차로 잡으므로 출근 후 첫 콜까지와 마지막 하차 뒤
퇴근까지가 빠진다. **실제 출차는 이보다 이르고 귀고는 이보다 늦다.** 조 편성을 5개 조로
묶는 규칙은 코드에 없다 — 자세한 내용은 [`preprocessing_log.md` B-7](preprocessing_log.md).

---

## 단계 → 모듈·함수 대응

| 단계 | 담당 모듈·함수 | 상태 |
|---|---|---|
| **조별 차고지 출차** | 거점 좌표·정원 `load.load_depots()` (44곳, 691대) / 조 편성 실측 `load.load_vehicle_trips()` → `metrics.vehicle_day_table()` | 입력은 있음. 차량을 조에 배정하는 로직은 `CallTaxiSim.__init__` — **미구현** |
| **콜 접수** | `load.load_calls()` — 즉시콜·서울 필터 후 1,527,213건. 각 행의 `received_at` 이 도착 이벤트, `origin_dong_canon`·`origin_lat/lon` 이 발생 위치 | 구현됨 |
| **점수제 대기열** | `CallTaxiSim.dispatch()` — 점수 = 거리 + 대기 + 접수. 탐색 반경 `DISPATCH_RADIUS_DAY = 7km` / `DISPATCH_RADIUS_NIGHT = 12km` | **미구현** (`raise NotImplementedError`). 반경 상수는 정의돼 있으나 코드 주석에 "미확정, 검증 필요" |
| **배차** | `CallTaxiSim.dispatch()` | **미구현** |
| **공차 이동** | `travel_time.get_travel_time(출발동, 목적동, hour, is_weekend)` — 대기 위치 동 → 승차지 동. 3단 폴백(실측 OD → 거리대 속도 → 시간대 평균속도), 반환값 1~120분 | 구현됨 |
| **승차** | `CallTaxiSim.vehicle_process()` | **미구현** |
| **운행** | `travel_time.get_travel_time()` — 승차지 동 → 목적지 동. 공차 이동과 같은 테이블을 쓴다 | 구현됨 |
| **하차** | `CallTaxiSim.vehicle_process()` | **미구현** |
| **하차지 인근 제자리 대기** | `CallTaxiSim.vehicle_process()` — 가정 A-01(유휴 차량 = 제자리 대기, 배차 콜이 타지역이면 공차 이동) | **미구현** |
| **취소 · 이탈** | 실측 판정은 `metrics.cancel_kind()` — 배차 전 + 1분 초과 취소를 `abandoned`(대기 중 포기)로 분류. 시뮬의 인내심 분포는 미정 | 실측 분류는 구현됨. 시뮬 이탈 로직은 **미구현** |
| **차고지 복귀** | `CallTaxiSim.vehicle_process()` | **미구현** |
| **하루 실행·로그 산출** | `CallTaxiSim.run()`, `run_placement(placement, calls, depots, travel_time, seed)` | **미구현** |
| **결과 채점** | `metrics.overall_metrics()` → `metrics.compare_to_target()` — 검증 6개 대조. 시뮬 로그도 실측 콜과 같은 함수를 통과한다 | 구현됨 |

### 구현 상태 요약

`src/simulator.py` 는 **뼈대만 있다.** `CallTaxiSim.__init__`, `dispatch`,
`vehicle_process`, `run`, `run_placement` 이 모두 `raise NotImplementedError` 다.
`src/dashboard.py` 도 같다.

즉 위 흐름도에서 **입력(콜·거점·이동시간)과 출력(지표 채점)은 구현돼 있고,
가운데 배차·운행 엔진이 비어 있다.** 배치안 평가 결과·before/after 비교값은 아직
산출된 것이 없으며, 현재 저장소의 모든 수치는 실측 진단값이다.

정의된 시뮬 상수는 `SEED = 42`, `DISPATCH_RADIUS_DAY = 7`, `DISPATCH_RADIUS_NIGHT = 12`
셋뿐이다.

---

## 모델 경계

흐름도가 다루지 **않는** 것 — 폭과 깊이를 가늠하는 데 필요하다.

| 항목 | 처리 |
|---|---|
| 예약콜 | 재생하지 않는다. 가용 차량에서 차감만 한다(가정 S-02). 즉시콜 필터로 원본의 11.68%(202,002건)가 입력에서 빠진다 |
| 서울 밖 출발 콜 | 입력에서 제외(261건) |
| 서울 밖 목적지 | **포함한다.** 운행은 재생하되 목적동 좌표가 없으면 이동시간 폴백으로 처리(전체의 7.26%) |
| 차량 고장·결행 | 모델링하지 않는다 |
| 기사 휴게 | 별도 활동으로 두지 않는다. 실측에서도 분리할 근거가 없다 |
| 억제된 수요 | 접수된 콜만 센다. 대기가 길어 아예 부르지 않게 된 수요는 어떤 지표로도 보이지 않는다 |
| 차량 유형 구분 | 원본에 `차량구분`(특장차/임차택시)·`차종` 이 있으나 배차 로직에서 구분하지 않는다 |
